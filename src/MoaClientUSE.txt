MoaClientUSE
东拼西凑调通了第一个请求，但是还有很多地方明显有问题，后面的扩展性非常差，需要一步步优化下去
使用的tcpdump命令
tcpdump -i eth0 port 6800 -X -s0

抓包工具使用
ip.addr == 200.200.169.212

踩过的坑
1 java转go时候massage其实就是一个pb对象，这个地方解析了很久，也就是现在的第三部分
2 直接用go还是需要往数组里面增加一个3的标志位，这个没有明白什么原因，但是加上以后发现能用，通过抓包工具抓取到的


3 发现登录成功了，但是后面创建工作汇报的请求怎么都不成功，各种调试
报文内容，所有的地方都对了，也发送了请求，单独发送创建请求也和java发送的一模一样，却还是不成功
1）调节发送顺序，没有效果
最后还是仔细查看每一个请求响应包的数据格式通过tcpdump命令查看
最后发现，登录请求的数据包长度多设置了两位，fuck，个人理解：按理上个包的请求长度，不会对下个请求包造成影响的，但是却还是受到了影响
（严格控制包的长度也是有必要的），除非是conn的最后一个请求包。 至此已经完成了业务请求
通过比较seq 和ack后面的数据得到的
真尼玛浪费时间





0901：已经可以发送工作汇报，登录成功，但是数组的位数还是不对，需要解决一下数组位数变化的情况
//数组的长度不够导致的问题
new 一个长点的数组就可以解决


