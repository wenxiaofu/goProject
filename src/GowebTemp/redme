执行一下这个，获取远程库失败了的话
如果已经有了下面的文件夹要把文件夹删除掉
go get github.com/gorilla/pat


还是有办法的
http://www.jianshu.com/p/05671bab2357


注释掉的内容
<!--
        {{range $i := .}}
            <td>{{$i}}</td>
        {{end}}

  --> 

  已经解决了的问题：
  1 可以传递参数了
  2 页面已经OK

 还需要解决的问题：
 1 使用ajax动态处理请求
 2 处理前端提交过来的数据，以及跳转的页面
 运行中和运行结束的处理
 还需要运行结果展示


ajax请求使用

能够直接发送一个pb，将pb转换成json，并不需要解析