下载驱动
go get github.com/go-sql-driver/mysql
用户名和密码
root  root

可以写一个数据库连接池但是不一定要用，需要时去查询就行了

PB怎么存到数据库表格中，难道给每一个模块都创建一个表，是否可以统一用一个表，统一保存为json格式的数据
方案一：都用同一个表，保存json格式，前段和服务端都解析json通过json传输
方案二：单独创建表，还是不可取
方案三：pb文件或者二进制直接存到数据库使用时解析出来，直接使用pb传输


1 需要解决的问题，数据库存储，前段兼容

二级优化：
1 第二个问题支持前段保存测试案例，根据服务器和
2 支持前段添加操作码和pb数据，自动解析，和生成db数据



扩张性，增加用户名和密码
增加测试案例
表结构：
1 测试案例模板
测试套件
测试案例
选择案例执行
查看和修改案例
增加测试案例

2 模板数据
自己设置每个字段：
字段
业务码	操作码	proto二进制/json数据  预留一个（直接保存成json）

模板可以添加到样板中

样板表
使用已经写好的
按模块区分


3 保存成测试案例
测试套件表
id suite
套件和案例关联表
ID suiteid caseid casename  

保存步数，每一步的操作，通过轮询实现
id suiteid id name step json
同一个id代表同一个测试案例
执行测试案例步骤
执行多个测试案例
外加一层

for int i:=1,i<len
	conn.write(step(i))
	conn.read(buf)


